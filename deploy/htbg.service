[Unit]
Description=HTBG Tracker (uvicorn)
After=network.target

[Service]
User=htbg
WorkingDirectory=/opt/htbg
ExecStart=/opt/htbg/.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
